general:
  ode_solver: numpy
  is_playback: true
  no_visual: false
  speedup: 5
  no_print: false
  sampling_time: 0.01
  time_start: 0.0
  time_final: 10.0
  save_animation: true
  penalty_param: 0.0
controller:
  _target_: ${rcognita:controllers.Controller}
  time_start: ${general.time_start}
  sampling_time: ${general.sampling_time}
  is_fixed_critic_weights: ${general.is_fixed_critic_weights}
speedup: ${general.speedup}
F_min: ${controller.sampling_time}
