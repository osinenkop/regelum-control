state_init: = numpy.array([${.r} * numpy.cos(${.theta}), ${.r} * numpy.sin(${.theta}), ${.phi}, 0., 0.]) 
action_init: = numpy.ones(${system.dim_input})

r: = numpy.random.uniform(0., 10.)
theta: = numpy.random.uniform(0., 2 * numpy.pi)
phi: = numpy.random.uniform(-20 * numpy.pi, 20 * numpy.pi)