state_init%%: = numpy.array([numpy.pi, -2.]) 
action_init: = numpy.ones(${system.dim_input})

state_init: = ${.state_init%%} + numpy.array([${.x1}, ${.x2}])

x1: = numpy.random.normal(0., 0.5)
x2: = numpy.random.normal(2., 0.5)