defaults:
  - system: inv_pendulum 
  - scenario: online 
  - simulator: casadi
  # - optional nominal_controller: nominal_${system}
  - controller: reinforce
  - running_objective: quadratic_${system}
  - initial_conditions: ic_${system}
  - observation_naming: naming_${system}
  - system_specific: spec_${system}
  # - optional animator: animator_${system}
  # - ${controller}_${system}

callbacks:
  - rcognita.callback.HistoricalObjectiveCallback
  - rcognita.callback.TotalObjectiveCallback
  # - rcognita.callback.CriticObjectiveCallback
  - rcognita.callback.CalfCallback
  - rcognita.callback.HistoricalObservationCallback
  # - rcognita.callback.QFunctionModelSaverCallback
  # - rcognita.callback.CalfWeightsCallback
  # - rcognita.callback.CriticWeightsCallback
  # - rcognita.callback.InspectReferrersCallback
  - rcognita.callback.ObjectiveLearningSaver


disallow_uncommitted%%: False
# mlflow_tracking_uri%%: ${oc.env:RCOGNITA_MLFLOW_TRACKING_URI}
# mlflow_artifacts_location%%: ${oc.env:RCOGNITA_MLFLOW_ARTIFACTS_LOCATION}

hydra:
  sweep:
    dir: ${oc.env:RCOGNITA_MULTIRUN_DIR}/${now:%Y-%m-%d}/${now:%H-%M-%S}_${controller.name%%}_${system_specific.name}