name: 2tank
target: regelum.system.TwoTankWithReference
observer: = regelum.observer.ObserverReference([0.4, 0.4])
dim_observation: = ${.target}().dim_observation
dim_action: = ${.target}().dim_inputs

sampling_time: 0.1
time_final: 80

action_bounds: = np.array([[0., 1.]])


