name: inv_pendulum
target: regelum.system.LunarLander
dim_observation: = ${.target}().dim_observation
dim_action: = ${.target}().dim_inputs

time_final: 10.
sampling_time: 0.01
action_bounds: = np.array([[-100., 100.],[-50, 50]])
